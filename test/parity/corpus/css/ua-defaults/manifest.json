{
  "id": "css/ua-defaults",
  "title": "a page that relies on UA defaults renders in the same colour and face on both halves",
  "covers": [
    "UA defaults",
    "inherited color",
    "default fonts"
  ],
  "gaps": [
    "P-119"
  ],
  "waitText": "defaults settled",
  "exclude": {
    "pixels": "the whole page is the divergence; a score would only restate the style dimension"
  }
}
